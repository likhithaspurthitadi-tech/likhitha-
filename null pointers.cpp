#include<stdio.h>
int main()
{
	int *ptr=NULL;
	printf("\n the value of the ptr %x",ptr);
	printf("\n the address of ptr %x",&ptr);
	printf("\n the ptr printing value %d",*ptr);
	return 0;
}
